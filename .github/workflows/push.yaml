on:
  push:
   branches:
     - "*"

jobs:
  run-validation:
    runs-on: ubuntu-latest
    permissions:
      contents: 'write'
      id-token: 'write'
    steps:
      - name: Validate JSON schema
        working-directory: ./
        run:  yarn && yarn test